[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rsmm"
version = "0.1.0"
description = "Ravenswatch Mod Manager — single-player modding tool for OEngine cooked assets and Lua scripting."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Ovilli" }]
requires-python = ">=3.11"
# Runtime is stdlib-only; tomllib (3.11+), http.server, zipfile, etc.
# No runtime dependencies are pulled from PyPI.
dependencies = []
keywords = ["ravenswatch", "modding", "oengine"]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Games/Entertainment",
]

[project.optional-dependencies]
dev = ["pytest>=7", "pytest-xdist>=3", "ruff>=0.4"]

[project.urls]
Homepage = "https://github.com/Ovilli/RavenswatchModManager"

[project.scripts]
rsmm = "rsmm.cli._dispatch:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --tb=short"
filterwarnings = ["ignore::DeprecationWarning"]
markers = [
    "slow: heavy binary roundtrip/codec tests; deselect with -m 'not slow'",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["rsmm*"]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = [
    "src/loader/third_party",
    # Ghidra Jython 2.7 post-scripts: run inside Ghidra with injected
    # globals (currentProgram, monitor, ...) — not py311 code.
    "docs/_re/scripts",
    # Vendored upstream GhidraMCP bridge (LaurieWired) — keep diffable.
    "tools/ghidra_mcp",
]

[tool.ruff.lint]
select = ["F", "E", "W", "I", "UP", "B", "BLE"]
ignore = ["F401"]   # unused imports: noisy on src/rsmm/__init__ re-exports

# Bare `except Exception:` swallows programmer errors and makes the CLI
# silently misbehave. New code is expected to catch specific exceptions.
# The files below contain pre-existing offenders that need a per-call
# audit before they can be narrowed without changing behaviour — they
# stay exempt here so CI passes today while still flagging brand-new
# violations elsewhere in the tree. Drop a path once its `except` calls
# are reviewed.
[tool.ruff.lint.per-file-ignores]
"src/rsmm/cli/cmd_log.py"      = ["BLE001"]
"src/rsmm/cli/json_bridge.py"  = ["BLE001"]
"tests/test_sdk.py"            = ["BLE001"]
"src/rsmm/cli/compat.py"       = ["BLE001"]
"src/rsmm/cli/doctor.py"       = ["BLE001"]
"src/rsmm/cli/lint.py"         = ["BLE001"]
"src/rsmm/cli/repo_cmd.py"     = ["BLE001"]
"src/rsmm/cli/run.py"          = ["BLE001"]
"src/rsmm/cli/safe_mode.py"    = ["BLE001"]
"src/rsmm/cli/sdk_doctor.py"   = ["BLE001"]
"src/rsmm/cli/test.py"         = ["BLE001"]

"src/rsmm/dev/class_diff.py"   = ["BLE001"]
"src/rsmm/dev/class_survey.py" = ["BLE001"]
"src/rsmm/manifest_graph.py"   = ["BLE001"]
"src/rsmm/sdk/config.py"       = ["BLE001"]
"src/rsmm/sdk/docs_gen.py"     = ["BLE001"]
"src/rsmm/sdk/health.py"       = ["BLE001"]
"src/rsmm/sdk/intermod.py"     = ["BLE001"]
"src/rsmm/sdk/plugins.py"      = ["BLE001"]
"src/rsmm/sdk/repo.py"         = ["BLE001"]
"src/rsmm/sdk/transaction.py"  = ["BLE001"]
"src/rsmm/sdk/versioning.py"   = ["BLE001"]

# Pre-existing offenders from the cook/uncook pipeline + roundtrip tests
# (blind try/except for skip-on-any-failure, plus dense binary-format code
# with packed statements). Exempt to keep CI green; narrow per-call when
# each is audited.
"src/rsmm/engine/cooked_schemas/animation.py" = ["E702", "E501"]
"src/rsmm/engine/magic_items.py"              = ["BLE001"]
"src/rsmm/engine/heroes.py"                   = ["BLE001"]
"src/rsmm/logging.py"                         = ["BLE001", "E501"]
"src/rsmm/cli/console_cmd.py"                 = ["BLE001", "E501"]
"src/rsmm/dev/ghidra_resolve.py"              = ["BLE001"]
"tests/test_animation_roundtrip.py"           = ["BLE001"]
"tests/test_asset_refs_roundtrip.py"          = ["BLE001"]
"tests/test_definitions_roundtrip.py"         = ["BLE001"]
"tests/test_entity_settings_roundtrip.py"     = ["BLE001"]
"tests/test_texture_roundtrip.py"             = ["BLE001"]

# One-off RE / discovery helpers in scripts/. Blind try/except is the
# intended skip-on-any-failure idiom while spelunking unknown binary
# layouts. These are now CI-linted (see ci.yml) so they can't rot, but
# the catch-all guards stay exempt like the cook/uncook pipeline above.
"scripts/extract_uncooked.py"                 = ["BLE001"]
"scripts/decode_gen_sidecars.py"              = ["BLE001"]
"scripts/gen2obj.py"                          = ["BLE001"]
