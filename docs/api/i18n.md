# rsmm.sdk.i18n

## `i18n.merge_bundles(bundles: 'list[I18nBundle]') -> 'dict[str, dict[str, str]]'`

Produce locale -> {namespaced_key: value} ready for text-bank merge.

Two mods can't collide because keys are namespaced per `mod_id`.
